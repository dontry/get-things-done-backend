{
  "type": "mongodb",
  "host": "localhost",
  "database": "gtd",
  "synchronize": true,
  "logging": true,
  "entities": ["src/api/models/*.ts"],
  "cli": {
    "entitiesDir": "src/api/models"
  }
}
