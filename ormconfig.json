{
  "type": "mongodb",
  "host": "localhost",
  "database": "test",
  "synchronize": true,
  "logging": false,
  "entities": ["src/api/models/*.ts"],
  "cli": {
    "entitiesDir": "src/api/models"
  }
}
